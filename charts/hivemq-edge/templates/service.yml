apiVersion: v1
kind: Service
metadata:
  name: {{ include "hivemq-edge.name" (dict "prefix" "hivemq-edge" "releaseName" .Release.Name) }}
  {{- with .Values.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "hivemq-edge.labels" . | nindent 4 }}
    {{- with .Values.serviceLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
    {{- if eq .Values.https.enable false }}
    - port: 8080
      name: http
    {{- end }}
    {{- if .Values.https.enable }}
    - port: 8443
      name: https
    {{- end }}
    {{- if .Values.mqtt.enable }}
    - port: 1883
      name: mqtt
    {{- end }}
    {{- if .Values.mqtts.enable }}
    - port: 8883
      name: mqtts
    {{- end }}
    {{- if .Values.mqttsn.enable }}
    - port: 2442
      name: mqttsn
    {{- end }}
  clusterIP: None
  selector:
    {{- include "hivemq-edge.selector-labels" . | nindent 4 }}
