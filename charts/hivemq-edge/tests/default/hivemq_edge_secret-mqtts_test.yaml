suite: HiveMQ Edge - Secret Mqtts tests

templates:
  - secret-mqtts.yml
release:
  name: edge
  namespace: hivemq-edge-namespace
chart:
  version: 0.1.0
  appVersion: "2025.1"

tests:
  - it: Secret should not be present
    asserts:
      - isNull:
          path: ""
